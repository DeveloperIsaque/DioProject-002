const NUMBERS = [1, 3, 4, 6, 80, 33, 23, 90];
const LENGTH_NUMBERS = NUMBERS.length;

for (let i = 0; i < LENGTH_NUMBERS; i++) {
    console.log((NUMBERS[i] % 2 === 0) ? NUMBERS[i] = 0 : NUMBERS[i]);
}

